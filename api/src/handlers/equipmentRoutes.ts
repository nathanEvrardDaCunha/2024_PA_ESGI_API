import express, {Request, Response} from "express";import {createEquipment, getAllEquipment, getEquipmentById, updateEquipment} from "../repository/equipmentsRepository";const equipmentRouter = express.Router();equipmentRouter.get('/', async (req: Request, res: Response) => {		try {		const equipments = await getAllEquipment();		res.status(200).json(equipments);	} catch (error) {		console.error('Error fetching equipments:', error);		res.status(500).json({error: 'Internal Server Error'});	}});equipmentRouter.get('/:id', async (req: Request, res: Response) => {		try {		const equipment = await getEquipmentById(req.params.id);		if (!equipment) {			return res.status(404).json({ error: 'Equipment not found' });		}		res.status(200).json(equipment);	} catch (error) {		console.error('Error fetching equipment by ID:', error);		res.status(500).json({ error: 'Internal Server Error' });	}});equipmentRouter.post('/', async (req: Request, res: Response) => {		try {		const newEquipment = await createEquipment(req.body);		res.status(201).json(newEquipment);	} catch (error) {		console.error('Error creating equipment:', error);		res.status(500).json({error: 'Internal Server Error'});	}});equipmentRouter.patch('/:id', async (req: Request, res: Response) => {		try {		const { id } = req.params;		const updatedEquipment = await updateEquipment(id, req.body);				if (!updatedEquipment) {			return res.status(404).json({ error: 'Equipment not found' });		}				res.status(200).json(updatedEquipment);	} catch (error) {		console.error('Error updating equipment:', error);		res.status(500).json({ error: 'Internal Server Error' });	}});export default equipmentRouter;